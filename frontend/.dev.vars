# Local env for Cloudflare Pages Functions (wrangler pages dev).
# The public linkblog functions (functions/blogs/*) read these.
# Local feed-proxy runs without a secret, so FEED_PROXY_SECRET is omitted here.
FEED_PROXY_URL=http://127.0.0.1:3000
